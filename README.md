# Quiz Bot (Python + uv)

A minimal Python project managed with **uv**, containing:

- A simple `hello()` function  
- A basic `main()` entry point  
- Automatic tests using **pytest**  
- Linting with **ruff**  
- Clean and modern project layout using the `src/` structure  

---

## 🚀 Getting Started

### Install dependencies

```bash
uv sync
